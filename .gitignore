/.bsp
/.ensime
/.ensime_lucene/
/.idea

*~
*.iml
*.sw[po]
*-old

.#*
.DS_Store
.history

/cypress/screenshots/
/cypress/videos/
/node_modules/

project/project/
target/
tmp/

CLAUDE.md
AGENTS.md
.claude/

# Per-deployment config — the live env file, sourced at launch (see hnw.env.example)
/hnw.env

# TLS keystore — never commit; supplied at runtime via HNW_KEYSTORE_PATH
/resources/*.p12
